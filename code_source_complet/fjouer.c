#include <stdio.h>
#include <stdlib.h>

void jouer (t_grille tab, char pion, int li, int col){
    choisirColonne()
    trouverLigne()
    int i, j, ligne;
    for (j = 0; j < NBLIG, j++)
    {
        for (i = 0; i < NBCOL; i++)
        {
            if (tab[i]!=VIDE)
            {
                ligne=tab[i]tab[j-1];

            }
        }
    } 
}